datatypes in javascript:
number: integer or floating-point, integers.
eg :  let n = 123;
      n = 12.345:

bigint:for integer numbers of arbitrary length.
eg :const bigInt = 1234567890123456789012345678901234567890n;

string : A string may have zero or more characters, there’s no separate single-character type.
eg:     let str = "Hello";
        let str2 = 'Hi Eveyone';

boolean : true/false.
eg:et nameFieldChecked = true; // yes, name field is checked

null :unknown values – a standalone type that has a single value null.
eg:let age = null;


undefined : unassigned values – a standalone type that has a single value undefined.
eg:If a variable is declared, but not assigned, then its value is undefined:
    let age;


object : complex data structures.
eg:   typeof 0 // "number"
      typeof true // "boolean"

symbol :unique identifiers.
eg:typeof Symbol("id") // "symbol"
